export type Tusers = {
    id: string,
    name: string,
    email: string,
    password: string,
    createdAt: string,
}

export type Tproducts = {
    id: string,
    name: string,
    price: number,   
    descripion: string,
    imgUrl: string,
}